# Playwright Rules

E2E tests run against real frontend, backend, and PostgreSQL. Prefer user-facing locators. Avoid arbitrary waits. Preserve traces/screenshots/videos on failure. Use isolated data and accounts. Critical tests must not be skipped. Map test IDs to requirement IDs, screen IDs, and operationIds.
