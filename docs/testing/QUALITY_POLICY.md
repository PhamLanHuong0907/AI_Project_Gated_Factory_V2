# Quality Policy

A stage passes only with executed evidence. Build success alone is not implementation coverage. Visual similarity requires direct comparison to approved Stitch screens. Playwright release tests must use real APIs. Graphify findings are advisory until verified.
