# Requirement Rules

Use MarkItDown MCP to convert source documents. Preserve original terminology. Every normalized requirement must have a stable ID. Do not silently fill gaps. Record assumptions and open questions. Each module must include overview, workflow, business rules, validations, permissions, reports, and acceptance criteria.
